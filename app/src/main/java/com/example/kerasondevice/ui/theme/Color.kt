package com.example.kerasondevice.ui.theme

import androidx.compose.ui.graphics.Color

// Keras brand palette — dark-first, red accent

// Primary brand
val KerasRed = Color(0xFFD00000)
val KerasRedDark = Color(0xFF9A0000)
val KerasRedContainer = Color(0xFF3A0000)

// Surfaces (dark)
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)
val SurfaceVariantDark = Color(0xFF2C2C2C)
val OutlineDark = Color(0xFF444444)

// Text (dark)
val OnSurfaceDark = Color(0xFFE0E0E0)
val OnSurfaceVariantDark = Color(0xFFA0A0A0)
val SecondaryTextDark = Color(0xFFB0B0B0)

// Terminal / output
val TerminalBackground = Color(0xFF0D0D0D)
val TerminalText = Color(0xFF4CAF50)

// Surfaces (light — secondary)
val BackgroundLight = Color(0xFFFAFAFA)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFEEEEEE)
val OutlineLight = Color(0xFFD0D0D0)
val OnSurfaceLight = Color(0xFF1A1A1A)
val OnSurfaceVariantLight = Color(0xFF666666)

// Status
val Success = Color(0xFF4CAF50)
val Error = Color(0xFFCF6679)
